services:
  - type: worker
    name: telegram-bot
    env: python
    plan: free
    buildCommand: |
      pyenv install 3.11.9
      pyenv global 3.11.9
      pip install -r requirements.txt
    startCommand: python bot.py